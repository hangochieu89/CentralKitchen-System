# Mặc định: H2 in-memory (không cần SQL Server). Xem application-local.properties.
# SQL Server: mvnw spring-boot:run "-Dspring-boot.run.profiles=sqlserver"
spring.profiles.active=local

server.port=8080
